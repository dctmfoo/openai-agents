node_modules
.env
.DS_Store
logs
dist

# Local memory / personal context (do not commit)
/MEMORY.md
/memory/

# Local runtime config (do not commit)
/config/*.json
!/config/*.example.json

# Compounding / automation artifacts (do not commit)
/tasks/**
/reports/**
/archive/**
/prd.json
/progress.txt
/compound-review.log
/auto-compound.log

# Ralph runner local state
/.ralph-worktrees/
/.ralph-sandboxes/

# Keep examples/templates committed
!/tasks/
!/tasks/prd.example.md
!/reports/
!/reports/example.md
!/prd.json.example

# apps/admin: per-app lockfile (keep root lockfile as source of truth)
/apps/admin/pnpm-lock.yaml
